# Description
Data and code to reproduce results on "Natural data structure explains the generalization gap in associative memories"

## PL library required
Please install in your environment PL library (with built-in pseudolikelihood training functions)

```
python -m pip install --user --no-cache-dir --upgrade pl-training
```
